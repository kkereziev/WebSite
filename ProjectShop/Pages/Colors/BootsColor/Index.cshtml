@page
@model ProjectShop.Pages.Colors.BootsColor.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Color of Boots</h1>
<form asp-page="./Index" method="get">
    <div class="form-actions no-color">
        <p>
            Find by color of dress or name of dress:
            <input type="text" name="SearchString" value="@Model.CurrentFilter" />
            <input type="submit" value="Search" class="btn btn-default" />
            <a asp-page="./Index"> Back to full List</a>
        </p>
    </div>
</form>
<p>
    <a asp-page="Create">Create New</a> |
    <a href="/ModelsOfShoes/ModelsOfBoots">Boots Models</a> |
    <a href="/Shos/Bots">Boots Brands</a> |
    <a href="/Sizes/SizeOfBots">Boots Sizes</a> |
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-page="./Index" asp-route-sortOrder="@Model.ColorOfBootsSort"
                   asp-route-currentFilter="@Model.CurrentFilter">
                    Color Of Boots
                </a>
            </th>
            <th>
                <a asp-page="./Index" asp-route-sortOrder="@Model.ModelIdSort"
                   asp-route-currentFilter="@Model.CurrentFilter">
                    @Html.DisplayNameFor(model => model.ColorOfBoots[0].Model)
                </a>
            </th>
            <th>
                <a asp-page="./Index" asp-route-sortOrder="@Model.BootsIdSort"
                   asp-route-currentFilter="@Model.CurrentFilter">
                    @Html.DisplayNameFor(model => model.ColorOfBoots[0].Model.Boots.Brand)
                </a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ColorOfBoots)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item._ColorOfBoots)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model.Boots.Brand)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@{
    var prevDisabled = !Model.ColorOfBoots.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.ColorOfBoots.HasNextPage ? "disabled" : "";
}
<a asp-page="./Index"
   asp-route-sortOrder="@Model.CurrentSort"
   asp-route-pageIndex="@(Model.ColorOfBoots.PageIndex - 1)"
   asp-route-currentFilter="@Model.CurrentFilter"
   class="btn btn-default @prevDisabled">
    Previous
</a>
<a asp-page="./Index"
   asp-route-sortOrder="@Model.CurrentSort"
   asp-route-pageIndex="@(Model.ColorOfBoots.PageIndex + 1)"
   asp-route-currentFilter="@Model.CurrentFilter"
   class="btn btn-default @nextDisabled">
    Next
</a>
