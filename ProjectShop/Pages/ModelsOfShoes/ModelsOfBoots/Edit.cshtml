@page
@model ProjectShop.Pages.ModelsOfShoes.ModelsOfBoots.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Boots Model</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BootsModel.Id" />
            <div class="form-group">
                <label asp-for="BootsModel.Name" class="control-label"></label>
                <input asp-for="BootsModel.Name" class="form-control" />
                <span asp-validation-for="BootsModel.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BootsModel.BootsId" class="control-label"></label>
                <select asp-for="BootsModel.BootsId" class="form-control" asp-items="ViewBag.BootsId"></select>
                <span asp-validation-for="BootsModel.BootsId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
